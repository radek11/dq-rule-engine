/**
 * What is checked: a rule's definition, how it maps a computed value to a decision, where it
 * applies, and how rules are catalogued and filtered.
 *
 * <p>Depends on {@code input} and {@code output}.
 */
package io.github.radek11.dq.rule;
