/**
 * How a run proceeds: {@link io.github.radek11.dq.RuleEngine} takes records from
 * {@code input}, evaluates the rules from {@code rule} and emits everything to {@code output}.
 *
 * <p>Depends on all three packages; nothing depends on it.
 */
package io.github.radek11.dq;
